# TODO 

## Improvements:

- YAML configuration by type should be easily extendable (remove Jackson annotations in PadlSourceConfig)
- PadlSourceConfig subclasses type field handling.
- Running config over an already configured target includes an empty schema
